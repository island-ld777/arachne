# cpp-crawler

A small C++ web crawler project built as a learning exercise.

## Goals
- Learn modern C++
- Practice HTTP fetching
- Extract links from HTML
- Build toward a real crawler architecture

## Build
```bash
mkdir build
cd build
cmake ..
cmake --build .